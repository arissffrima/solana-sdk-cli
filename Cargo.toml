[package]
build = "build.rs"
name = "solana-sdk-cli"
version = "0.1.0"
edition = "2021"
authors = ["Vault Labs"]
license = "MIT"
description = "Solana SDK-shaped CLI — path 501, stub lamports."
readme = "README.md"
repository = "https://github.com/vaultlabs/solana-sdk-cli"

[[bin]]
name = "solana-sdk-cli"
path = "src/main.rs"
